<UserControl x:Class="InitiativeTracker.MVVM.Views.CombatantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             d:DataContext="{Binding CombatantViewModel, Source={StaticResource Locator}}"
             mc:Ignorable="d" 
             d:DesignHeight="100" Width="435">
    <Grid x:Name="CombatantGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"/>
            <ColumnDefinition Width="100*"/>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>
    	<TextBlock Grid.Column="0" TextWrapping="Wrap" Text="{Binding DisplayName}"/>
    	<TextBlock Grid.Column="1" TextWrapping="Wrap" Text="{Binding Initiative}"/>
    	<TextBlock Grid.Column="2" TextWrapping="Wrap" Text="{Binding Modifier}"/>
    </Grid>
</UserControl>
